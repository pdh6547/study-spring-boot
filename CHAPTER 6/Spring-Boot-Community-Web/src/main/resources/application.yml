spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/jpa_ex?useSSL=false
    username: root
    password: donghyun0625
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true
      path: /console
  devtools:
    livereload:
      enabled: true
  thymeleaf:
    cache: false
  security:
    oauth2:
      client:
        registration:
          github:
            client-id:
            client-secret:
          google:
            client-id:
            client-secret:
          facebook:
            client-id:
            client-secret:

custom:
  oauth2:
    kakao:
      client-id: